from thesaurus import Word

sentence = input('Enter sentence: ')

word = input('Enter word to analyze: ')

th = Word(word)

for w in th.synonyms():
    new_sentence = sentence.replace(word, w)
    print(new_sentence)
